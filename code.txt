main = tkinter.Tk()

main.geometry("600x400")

username = tkinter.StringVar()
password = tkinter.StringVar()

def submit():
     name_var = username.get()
     key_var = password.get()

     users.append(name_var)
     pins.append(name_var)

     username.set("")
     password.set("")

tkinter.Label(main, text='Username').grid(row=0)
tkinter.Label(main, text='PIN').grid(row=1)

e1 = tkinter.Entry(main, textvariable = username)
e2 = tkinter.Entry(main, textvariable = password, show='*')
btn = tkinter.Button(main, text='Submit', command=submit)

e1.grid(row=0, column=1)
e2.grid(row=1, column=1)
btn.grid(row=2, column=1)